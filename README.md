React-Native Expo using TensorFlow and MobileNet models to classify objects in an image.

```
git clone https://github.com/seanconrad1/RNImageReconginitionTest.git
cd RNImageReconginitionTest
yarn install
expo start
```
